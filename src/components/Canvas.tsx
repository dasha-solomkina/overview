import { Paper } from '@mui/material'

const Canvas = () => {
  return (
    <Paper
      sx={{
        // bgcolor: 'blue',
        display: 'flex',
        flex: 1,
        borderRadius: 2
      }}
    >
      Canvas
    </Paper>
  )
}

export default Canvas
