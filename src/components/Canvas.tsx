import { Paper } from '@mui/material'

const Canvas = () => {
  return (
    <Paper
      sx={{
        display: 'flex',
        flex: 1,
        borderRadius: 2,
        padding: 2,
      }}
    >
      Canvas placeholder
    </Paper>
  )
}

export default Canvas
